# Run from this directory
cd ${0%/*} ||exit 1     

# Create a build directory
mkdir build             

# Generate a project buildsystem (a modern way)
cmake -H. -S. -Bbuild  

# Build a project
cmake --build build
